# AGENTS_CUSTOM.md

## z.ai Provider Notes
- z.ai unit codes match CodexBar: `1` days, `3` hours, `5` minutes, `6` weeks.
- `unit=6, number=7` means 7 weeks (`70560` minutes), not 7 days.
- z.ai `TIME_LIMIT` with `unit=5, number=1` is monthly marker and should display `Monthly`.
